# 1. Le nom de ce service
spring.application.name=gateway-service

# 2. L'adresse de votre Config Server (Étape 1)
spring.cloud.config.uri=http://localhost:8888

# 3. L'adresse de votre Eureka Server (Étape 2)
# On le met ici pour qu'il le sache avant même de démarrer
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/