server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_paiement?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
